import 'package:flutter/material.dart';

class CreateDebatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Create a Debate"),
      ),
    );
  }
}
